algorithm: dsgd
total_rounds: 500
local_steps: 1
batch_size: 64

optimizer:
  lr: 0.1
  momentum: 0.9
  weight_decay: 1.0e-4

lr_schedule:
  type: cosine
  lr_min: 1.0e-4
  warmup_rounds: 5

eval_every: 10
checkpoint_every: 50
